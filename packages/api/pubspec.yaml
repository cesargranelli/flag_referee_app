name: flag_api
description: Cliente HTTP (dio) e serviços da API REST do Flag Platform.
version: 0.3.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3
  flag_core: ^0.3.0
  flag_domain: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
